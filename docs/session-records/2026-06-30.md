# 세션 기록 — 2026-06-30

> 브랜치: `migrate/playboard-from-prototype` · 작성자: Ella + Claude

## 오늘 한 일

### 1. PR #216 — 프로젝트 1차 베이스라인을 main 으로
- 현재 브랜치가 main 보다 3커밋 앞서 있고 미병합인 것을 확인.
  - `89f0451` 스캐폴드 / `ce45509` 기반 레이어(CT-API 001~011 + Prisma) / `379b7fc` 프로토타입 UI + PlayBoard SoT.
- 변경 요약 정리 + **PR #216** 생성 (→ main).
- 검증 게이트 통과: `npm test` 86/86 · `npm run build` 45/45 라우트 · `playboard-integrity` **GO**(불변식 7/7).
- **중간 사고**: 작업 중 누군가 main 에 **PR #207(`37dc0b9`)** 머지 — 내 `ce45509` 와 **트리 완전 동일**한 기반 레이어. GitHub 이 CONFLICTING 판정.
  - 해결: 브랜치를 `origin/main` 위로 **rebase** → 중복 `ce45509` 빈 커밋으로 자동 제거 → 새 작업 `b7a8efe`(프로토타입+PlayBoard) 1커밋만 남김 → `--force-with-lease` push.
  - rebase 후 재검증 86/86 green, PR 상태 **MERGEABLE / CLEAN** 확인.
- **상태: 머지 대기** — 머지 버튼은 사용자가 직접 누르기로 함. 단일 PR이라 순서 의존성 없음. 권장 방식 Rebase/Squash.

### 2. W10(인증 백엔드, Kakao OAuth) 의존 순서 분석
관련 task 정독: FW-AUTH-001~006, CT-DB-002/011, IF-SUP-001, TS-UT-001/002.
- **Supabase DB 연결은 W10 밖 선행(IF-SUP-001)**. provider=postgresql 고정(D7)이라 연결정보 없이는 `prisma migrate`도 불가.
- `/api/health/db` 코드는 이미 있음(CT-DB-001)이나 실 DB 붙기 전까진 503.
- 위상 정렬 순서 확정:
  ```
  IF-SUP-001(선행) → CT-DB-002(User) → FW-AUTH-001(SSR 클라이언트)
    → FW-AUTH-002(가입) → FW-AUTH-004(콜백) → FW-AUTH-003(로그인)
    → FW-AUTH-006(Kakao) → FW-AUTH-005(환경설정)
  ```
- 주의 2가지:
  1. **CT-DB-011(RLS)는 전체를 지금 못 끝냄** — 모든 모델(CT-DB-001~010) 의존. W10 에선 **User 테이블 RLS만**, 나머지는 W11/W13/W14 에서 완성.
  2. **FW-AUTH-005 는 FR-AUTH-002(`requireUser` 가드) 선행** 필요 — 로그인 직후·환경설정 직전에 인증 read 레이어(FR-AUTH-001/002) 삽입.

### 3. IF-SUP-001 + Kakao 셋업 체크리스트 작성
- `docs/SETUP_IF-SUP-001_Kakao.md` (커밋 `6517f9c`, PR #216 에 포함) — STEP 1~12, 사이트·입력값·환경변수 매핑까지 명시.
- `.env.local` 자리표시 생성(gitignore 유지) — 각 변수에 출처 STEP 주석.

### 4. (곁가지) Hermes 질문
- Hermes = Meta 의 React Native용 JS 엔진(AOT 바이트코드·저메모리·빠른 시작). **이 프로젝트(Next.js 웹)엔 적용 불가/무의미** — 네이티브 모바일 앱 낼 때만 의미. 결론만 공유, 코드 변경 없음.

## 진행 중(사용자) — Supabase staging 셋업
- `.env.local` 에 staging DB 2종 + Supabase API 3종 입력 작업 진행 중.

## ⚠️ 다음 세션 시작 시 먼저 확인할 것
1. **`NEXT_PUBLIC_SUPABASE_URL` 값 점검** — 체크리스트 STEP 5-C 기준 값은 `https://<ref>.supabase.co` 형태여야 함. 현재 `.env.local` 값에 `/rest/v1/` 가 붙어 있으면 **Supabase 클라이언트가 깨짐** → 접미사 제거 필요.
2. **PR #216 머지 여부** — 아직이면 머지 후 main 기준으로 W10 브랜치 생성.
3. **main 브랜치 보호 미설정** — #207/#216 중복 사고 재발 방지용으로 보호 규칙(PR 필수) 설정 권장.

## 다음 시작점 (착수 조건 충족 시)
- 조건: `.env.local` staging 5개(DB 2 + Supabase 3) 채워짐 + URL 형식 정상.
- 검증: `npm run dev` → `GET /api/health/db` 가 `{ ok: true }`.
- 착수: **`CT-DB-002`(User 모델 + 4 enum + migrate)** → **`FW-AUTH-001`**(SSR 클라이언트 3종 + middleware 세션갱신 병합 + env 검증 + `/api/health/auth`).
- Kakao(FW-AUTH-006)는 그 한참 뒤 단계 — 외부 등록은 그 전까지만 끝내면 됨.
</content>
